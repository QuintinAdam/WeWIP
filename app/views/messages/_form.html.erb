<% if can? :create, Message %>

  <turbo-frame id="<%= dom_id(@project, :form) %>">
    <%= form_for [@project, Message.new] do |f| %>
      <%= f.text_field :content, placeholder: "Message" %>
      <%= f.button "Send", class: 'btn btn-primary' %>

    <% end %>
  </turbo-frame>

<% end %>
